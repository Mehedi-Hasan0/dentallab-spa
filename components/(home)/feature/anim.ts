import { staggerContainer, itemFadeUp } from '@/components/shared/sharedAnim';

export const featureContainerVariants = staggerContainer(0.1, 0.1);
export const featureItemVariants = itemFadeUp(30, 0.6);
