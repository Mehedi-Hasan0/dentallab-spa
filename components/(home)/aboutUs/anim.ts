import { staggerContainer, itemFadeUp } from '@/components/shared/sharedAnim';

export const aboutUsContainerVariants = staggerContainer(0.15, 0.1);
export const aboutUsItemVariants = itemFadeUp(30, 0.6);
